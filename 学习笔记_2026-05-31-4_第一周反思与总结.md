# 学习笔记 2026-05-31-4：第一周反思与概念图

## 日程回顾

| 天 | 主题 | 核心收获 |
|---|------|----------|
| Day 1 | 训练流程骨架 | compile → fit → predict 三件套 |
| Day 2 | 训练/测试集分离 + 过拟合初体验 | 训练集 loss 降、测试集 loss 涨 = 过拟合 |
| Day 3 | 激活函数 ReLU | 多层 Dense 无激活 = 一层 Dense；ReLU 在 0 处掰弯打破线性 |
| Day 4 | 隐藏层 | 层 1 提取基础特征 → 层 2 组合特征 → 输出层分类 |
| Day 5 | MNIST 加载 + 归一化 | Flatten 拍平 28×28→784，归一化 /255，学习率从 0.1→0.001 |
| Day 6 | 训练 MNIST + 评估 | Dropout 防过拟合 + EarlyStopping 自动停 |
| Day 7 | 反思巩固 | 拆零件实验 + 找 bug + 费曼——检验真懂 |

---

## 核心概念图（一张图串起第一周）

```
                    ┌── 训练集（用来学）
    数据 ──────────┤
                    └── 测试集（用来考）

                    ┌── /255.0 归一化
    预处理 ────────┤
                    └── Flatten → 784 维向量

                    ┌── Dense(神经元数)     ← 线性变换 Wx+b
    网络层 ────────┤
                    ├── ReLU()              ← 非线性掰弯（打破线性叠加）
                    ├── Dropout(比例)       ← 随机关神经元防背诵
                    └── Dense(分类数)       ← 输出 raw logits

                    ┌── Adam(learning_rate) ← 优化器，lr 太大会跨过最优
    编译 compile ──┤
                    └── SparseCategoricalCrossentropy(from_logits=True)
                                             ← 损失=量尺，True 表示内部做 softmax

                    ┌── validation_data      ← 每个 epoch 看测试集表现
    训练 fit ──────┤
                    ├── EarlyStopping(patience) ← val_loss 不降就停
                    └── 监控 train_loss vs val_loss gap

                    ┌── argmax 取最大概率索引
    预测 predict ──┤
                    └── 和真实标签比准确率
```

---

## 本周踩过的坑

| 坑 | 教训 |
|----|------|
| Dense(3) → 多层 Dense 叠起来还是线性 | 必须加激活函数，否则多层=一层 |
| Dense(4) 处理 784 维数据 → loss 卡在 1.7 | 神经元太少 = 信息瓶颈 |
| learning_rate=0.1 用 Adam 跑 MNIST → 准确率 40% | 合理起点是 0.001 |
| 两层 128 不如一层 128 | MNIST 不需要太深，复杂度匹配任务 |
| softmax + from_logits=True 混用 | 二选一：要么 softmax 层，要么 from_logits=True |

---

## 自我能力评估（5/31 独立默写验证）

✅ **已掌握：**
- 独立写出完整 MNIST 训练流程（加载→归一化→Flatten→Dense→ReLU→Dropout→Dense→compile→fit→predict）
- 理解过拟合现象和 Dropout/EarlyStopping 的用法
- 理解 from_logits 和 softmax 的关系

⬜ **还需加强：**
- 用通俗语言解释概念（费曼）
- 更多变种实验积累直觉（不同学习率、层数、神经元数的效果）

---

## 下周预告：CNN 卷积神经网络

告别「把图片当 784 个独立数字」的阶段——卷积真正学会「看形状」：

> Dense：每个像素一个独立权重
> CNN：滑动窗口看局部区域，共享权重

Day 8 见！
