# 学习笔记 2026-05-31-3：Dense 网络训练 MNIST + 评估

## 一句话总结

训练 MNIST 不是"代码能跑"就完了——要监控训练/验证 loss 防止过拟合，用 Dropout + EarlyStopping 做正则化。

---

## 核心实验：从"能跑"到"能防过拟合"

### 第一版：纯训练（Day 5）

```python
model.fit(train_images, train_labels, epochs=20)
```

结果：

```
epoch   train_loss    val_loss
  1      0.2528        0.1444
  6      0.0356        0.0780
 11      0.0129        0.0842
 20      0.0045        0.0964
```

**训练 loss 一路降，验证 loss 从 epoch 6 触底后反弹**——经典过拟合。

### 第二版：加 Dropout + EarlyStopping

```python
model = Sequential([
    Flatten(input_shape=(28, 28)),
    Dense(128),
    ReLU(),
    Dropout(0.2),      # ← 随机关掉 20% 神经元
    Dense(10)
])

model.fit(..., callbacks=[
    EarlyStopping(monitor='val_loss', patience=3)  # ← 3 轮不降就停
])
```

结果：

```
Epoch 1:  train_l=0.2984, val_l=0.1444
Epoch 3:  train_l=0.1049, val_l=0.0899
Epoch 5:  train_l=0.0716, val_l=0.0722
Epoch 7:  train_l=0.0573, val_l=0.0614  ← 最佳
Epoch 10: train_l=0.0436, val_l=0.0642  ← 停止
```

| | 无防御 | 有防御 |
|---|--------|--------|
| 最佳 val_loss | 0.078（epoch 6） | **0.061（epoch 7）** |
| 最终 gap | 0.09 | **0.02** |
| 准确率 | 97.7% | **98.05%** |
| 训练轮数 | 20（跑满） | 10（自动停） |

---

## 两个武器回顾

### Dropout

- 每次训练随机关掉一部分神经元（这里是 20%）
- 强制网络不能依赖某几个特定的神经元来"背"训练集
- **只在训练时生效**，预测时全部神经元参与
- 像一个老师逼学生不止记一个知识点，要每个点都会

### EarlyStopping

- 监控验证 loss，连续 N 轮不降就停
- `patience=3` = 允许等 3 轮
- 防止验证 loss 重新涨上去

---

## 第一周反思

六天前 vs 今天：

| 六天前 | 今天 |
|--------|------|
| 不知道怎么训练模型 | 能搭 Dense 网络训练 MNIST 到 98% |
| 以为层越多越好 | 知道要匹配任务复杂度 |
| 随便设学习率 | 知道 0.001 是 Adam 的合理起点 |
| 不检查过拟合 | Dropout + EarlyStopping + val_loss 监控 |

### 第一周核心概念图

```
数据 → Flatten → Dense → ReLU (→ Dropout) → Dense → 输出
                    ↑                ↑
              线性变换          非线性掰弯
                    ↑
         多层组合 = 特征提取层次（隐藏层）

训练闭环：
  compile(损失函数 + 优化器 + 学习率)
  → fit(数据, 验证集, callbacks=[EarlyStopping])
  → 监控 train_loss vs val_loss，gap 大了就是过拟合
```
